module.exports = {
  /**
   * {Number} The maximum limit (page size).
   */
  MAX_LIMIT: 30000,

  /**
   * {Number} The default limit (page size), if none is specified.
   */
  DEFAULT_LIMIT: 250
};
